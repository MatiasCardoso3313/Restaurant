{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/juego",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "stopOnEntry": false,
        }
    ]
}